
// ------------------------------------------------------------------------------- //
// RUN AFTER DOM LOADS                                                             //
// ------------------------------------------------------------------------------- //

$('document').ready(function () {

    // $('#Chapter').val('');

    // Load rich text editor
    CKEDITOR.replace('Content', {
    });
});